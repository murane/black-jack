import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

public class UserTest {

    @Test
    void testUserGetDraw() {
        /*
        User user = new User();
        Deck deck = new Deck();

        user.draw(deck);
        Assertions
        .assertEquals(user.cards.size(), 1);
         */
    }

}
